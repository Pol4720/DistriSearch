# =============================================================================
# DistriSearch Agent - Configuración de Variables de Entorno
# =============================================================================
# Copia este archivo como .env y ajusta los valores según tu entorno

# -----------------------------------------------------------------------------
# CONFIGURACIÓN DEL AGENTE
# -----------------------------------------------------------------------------

# URL del backend central
BACKEND_URL=http://localhost:8000

# Nombre del nodo agente
NODE_NAME=Agent-1

# Puerto del servidor de archivos del agente
FILE_SERVER_PORT=5001

# -----------------------------------------------------------------------------
# CARPETA COMPARTIDA
# -----------------------------------------------------------------------------

# Ruta a la carpeta compartida que el agente monitorizará
SHARED_FOLDER=./shared_folder

# -----------------------------------------------------------------------------
# CONFIGURACIÓN SSL/TLS (HTTPS)
# -----------------------------------------------------------------------------

# Habilitar SSL/TLS en el servidor de archivos del agente
AGENT_SSL_ENABLED=false

# Rutas a los certificados SSL del agente
# Usa los mismos certificados generados para el backend o genera unos específicos
AGENT_SSL_CERT_FILE=../certs/distrisearch.crt
AGENT_SSL_KEY_FILE=../certs/distrisearch.key

# -----------------------------------------------------------------------------
# VERIFICACIÓN DE CERTIFICADOS (al conectar con backend HTTPS)
# -----------------------------------------------------------------------------

# Verificar certificado SSL del backend (false para certificados autofirmados)
# En producción con certificados válidos, cambiar a true
VERIFY_SSL=false

# -----------------------------------------------------------------------------
# INTERVALOS Y TIMEOUTS
# -----------------------------------------------------------------------------

# Intervalo de escaneo de archivos (en segundos)
SCAN_INTERVAL=300

# Timeout de heartbeat (en segundos)
HEARTBEAT_INTERVAL=60

# -----------------------------------------------------------------------------
# EJEMPLO DE CONFIGURACIÓN CON HTTPS
# -----------------------------------------------------------------------------

# Descomenta y ajusta para conectar con backend HTTPS:
# BACKEND_URL=https://192.168.1.100:8000
# AGENT_SSL_ENABLED=true
# AGENT_SSL_CERT_FILE=../certs/distrisearch.crt
# AGENT_SSL_KEY_FILE=../certs/distrisearch.key
# VERIFY_SSL=false  # Mantener en false si usas certificados autofirmados

# NOTAS:
# 1. Si el backend usa HTTPS, actualiza BACKEND_URL a https://
# 2. Si AGENT_SSL_ENABLED=true, el agente servirá archivos por HTTPS
# 3. VERIFY_SSL=false es necesario para certificados autofirmados
