# Upstream generado automáticamente - mié 14 ene 2026 12:28:59 CST
# Nodos detectados: node-1 node-2 

# API endpoints - todos los nodos activos
upstream node_api {
    least_conn;
    server node-1:8000 max_fails=3 fail_timeout=10s;
    server node-2:8000 max_fails=3 fail_timeout=10s;
    keepalive 16;
}

# Frontend endpoints
upstream node_frontend {
    least_conn;
    server node-1:80 max_fails=3 fail_timeout=10s;
    server node-2:80 max_fails=3 fail_timeout=10s;
    keepalive 8;
}

# Para compatibilidad con configuración existente
upstream master_api {
    least_conn;
    server node-1:8000 max_fails=3 fail_timeout=10s;
    server node-2:8000 max_fails=3 fail_timeout=10s;
    keepalive 16;
}

upstream slave_api {
    least_conn;
    server node-1:8000 max_fails=3 fail_timeout=10s;
    server node-2:8000 max_fails=3 fail_timeout=10s;
    keepalive 16;
}

upstream slave_frontend {
    least_conn;
    server node-1:80 max_fails=3 fail_timeout=10s;
    server node-2:80 max_fails=3 fail_timeout=10s;
    keepalive 8;
}
