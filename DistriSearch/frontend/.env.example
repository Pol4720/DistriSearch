# =============================================================================
# DistriSearch Frontend - Configuración de Variables de Entorno
# =============================================================================
# Copia este archivo como .env y ajusta los valores según tu entorno

# -----------------------------------------------------------------------------
# CONEXIÓN AL BACKEND
# -----------------------------------------------------------------------------

# URL del backend (IMPORTANTE: Usa la IP de red, no localhost, para acceso externo)
# Ejemplos:
#   - Desarrollo local: http://localhost:8000
#   - Red local HTTP: http://192.168.1.100:8000
#   - Red local HTTPS: https://192.168.1.100:8000
DISTRISEARCH_BACKEND_URL=http://localhost:8000

# URL pública del backend (para enlaces de descarga accesibles desde otras máquinas)
# Debe ser la misma IP/hostname por la que otras computadoras acceden al backend
# Si está vacío, se intentará detectar automáticamente
DISTRISEARCH_BACKEND_PUBLIC_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# AUTENTICACIÓN
# -----------------------------------------------------------------------------

# API Key para operaciones administrativas (si el backend la requiere)
DISTRISEARCH_ADMIN_API_KEY=

# -----------------------------------------------------------------------------
# CONFIGURACIÓN DE LA APLICACIÓN
# -----------------------------------------------------------------------------

# Puerto del frontend Streamlit
STREAMLIT_PORT=8501

# -----------------------------------------------------------------------------
# EJEMPLO PARA RED LOCAL CON HTTPS
# -----------------------------------------------------------------------------

# Descomenta y ajusta para entorno de red local con HTTPS:
# DISTRISEARCH_BACKEND_URL=https://192.168.1.100:8000
# DISTRISEARCH_BACKEND_PUBLIC_URL=https://192.168.1.100:8000

# NOTAS IMPORTANTES:
# 1. Reemplaza 192.168.1.100 con la IP real de la máquina que ejecuta el backend
# 2. Para encontrar tu IP en Windows: ipconfig
# 3. Para encontrar tu IP en Linux/Mac: ifconfig o ip addr
# 4. Si usas HTTPS con certificados autofirmados, el navegador mostrará advertencia
# 5. Los enlaces de descarga deben usar la misma URL pública configurada aquí
